# TOOLS 本地化覆盖（zh-CN）

## 设计原则

- 最小权限、可审计、可确认、可回滚。

## 默认允许

- 查询类工具、报告类工具、受路径约束的记忆读写。

## 需确认后执行

- `jira.create`、`github.close_pr`、批量状态更新、广播消息。

## 默认禁止

- `shell.exec`、未限定路径写入、删除类操作、非幂等同步写入。

## 幂等与审计约束

- 以 `external_id` 去重，写操作记录 `audit.log`。

